

apiVersion: v1
kind: Secret
metadata:
  name: ml-platform-secrets
  namespace: ml-platform
  labels:
    app: ml-platform
type: Opaque
data:
  # Redis password (base64 encoded "changeme123")
  # Usage: REDIS_PASSWORD environment variable
  redis-password: Y2hhbmdlbWUxMjM=

  # API authentication token (base64 encoded "demo-token-12345")
  # Usage: For authenticating job submission requests
  api-token: ZGVtby10b2tlbi0xMjM0NQ==

  # HuggingFace token (base64 encoded "hf_demo_token")
  # Usage: For downloading private models
  huggingface-token: aGZfZGVtb190b2tlbg==

---